%section{:id => "rail-lines"}
    .panel.panel-default
        .panel-heading
            %h2.panel-title DCMETRO RAIL LINES
        .panel-body
            %ul
                -@lines.each do |line|
                    %li= line["DisplayName"]